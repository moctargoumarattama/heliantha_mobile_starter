Je veux maintenant optimiser UNIQUEMENT les images produit Heliantha.

Ne touche pas au cache général, à la navigation, au catalogue,
au panier ou à l'architecture actuelle.

OBJECTIF :
ne plus télécharger une grande image PrestaShop pour afficher
une petite carte Flutter.

1. Vérifier comment l'endpoint image actuel fonctionne :
   /v1/products/{id}/image?image_id=...

2. Vérifier si le PrestaShop Heliantha expose des formats d'image
   redimensionnés existants (small_default, medium_default,
   home_default, etc.) via son Webservice / URLs images.

3. Pour ProductCard :
   utiliser une miniature adaptée à environ 150-300 px,
   pas l'image originale si une miniature existe.

4. Pour le slider accueil :
   utiliser une image plus grande adaptée au slider.

5. Pour la fiche produit :
   conserver une image haute résolution.

Donc idéalement :
LISTE -> thumbnail
SLIDER -> medium
FICHE PRODUIT -> large/original

6. Ne jamais modifier les images originales PrestaShop.
7. Ne jamais stocker les images dans Flutter.
8. Garder loadingBuilder/errorBuilder.
9. Garder la compatibilité Flutter Web + Android.

MESURER :
- dimensions des images
- taille transférée approximative
- temps d'affichage avant/après

Tester sur :
panneau
onduleur
batterie
groupe électrogène

Puis :
flutter analyze
pytest

À la fin donne seulement :
- méthode d'image utilisée actuellement
- formats PrestaShop disponibles
- fichiers modifiés
- poids avant/après
- temps avant/après
- résultat tests